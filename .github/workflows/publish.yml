name: Publish avoid-nodes-edge to npm

on:
  push:
    branches:
      - main
    paths:
      - "packages/avoid-nodes-edge/**"
  workflow_dispatch:

# Fixes the GitHub Actions deprecation warning cleanly
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  publish:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: packages/avoid-nodes-edge

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          registry-url: "https://registry.npmjs.org"

      - name: Install dependencies
        run: npm install
        working-directory: . # Installs at the root level for monorepos

      - name: Build package
        run: npm run build

      - name: Check if version changed
        id: version-check
        run: |
          PACKAGE_NAME=$(node -p "require('./package.json').name")
          LOCAL_VERSION=$(node -p "require('./package.json').version")
          
          # FIXED: Prevent bash from crashing (exit code 1) if npm view fails
          PUBLISHED_VERSION=$(npm view "$PACKAGE_NAME" version 2>/dev/null || true)
          if [ -z "$PUBLISHED_VERSION" ]; then
            PUBLISHED_VERSION="0.0.0"
          fi
          
          echo "local=$LOCAL_VERSION" >> $GITHUB_OUTPUT
          echo "published=$PUBLISHED_VERSION" >> $GITHUB_OUTPUT
          
          if [ "$LOCAL_VERSION" = "$PUBLISHED_VERSION" ]; then
            echo "changed=false" >> $GITHUB_OUTPUT
            echo "Version $LOCAL_VERSION already published — skipping."
          else
            echo "changed=true" >> $GITHUB_OUTPUT
            echo "Publishing $PACKAGE_NAME@$LOCAL_VERSION (was $PUBLISHED_VERSION)"
          fi

      - name: Publish to npm
        if: steps.version-check.outputs.changed == 'true'
        run: npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}